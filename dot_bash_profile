# .bash_profile

# For MacOS
if [[ "$OSTYPE" == "darwin"* ]]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
  . ~/.bashrc
fi

if [[ "$OSTYPE" == "linux-gnu"* && -z "$SSH_AUTH_SOCK" ]]; then
  eval "$(ssh-agent -s)"
fi
